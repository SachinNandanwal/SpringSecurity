Spring security configuration
-----------------------------

1. Add the spring security dependency
- After adding the dependency by default a security mechanism will be applied to the application
- a default user will be created and the userName will be : "user"
-                                    userPassword will be : <displayed in the console>